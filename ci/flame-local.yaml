---
current-context: flame
contexts:
  - name: flame
    cluster:
      endpoint: "http://127.0.0.1:8080"
    cache:
      endpoint: "grpc://127.0.0.1:9090"
    package:
      excludes:
        - "*.log"
        - "*.pkl"
        - "*.tmp"
        - ".venv"
        - "__pycache__"
        - ".pytest_cache"
        - ".ruff_cache"
        - "*.egg-info"
        - "dist"
        - "build"
        - ".git"