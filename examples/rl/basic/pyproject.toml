[project]
name = "rl"
version = "0.2.0"
description = "Distributed Reinforcement Learning Example using Flame App"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "torch",
    "numpy",
    "gymnasium[mujoco]",
]

[project.optional-dependencies]
plot = ["matplotlib"]

[dependency-groups]
dev = ["flamepy"]

[tool.setuptools]
py-modules = ["distributed", "main", "model"]
